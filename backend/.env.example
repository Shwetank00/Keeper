# ===== JWT / Auth =====
ACCESS_TOKEN_SECRET=change_me_to_a_long_random_string
# Optional if your code uses it:
# ACCESS_TOKEN_EXPIRES=1d

# ===== SMTP (Nodemailer) =====
# If using Gmail, keep host/port/secure as below.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_gmail_app_password   # app password, not your real login

# ===== Database =====
# Use whichever key your code expects (keep ONE; delete the other).
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority&appName=Keeper
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority&appName=Keeper

# ===== CORS =====
# Comma-separated list of allowed origins
FRONTEND_URL=http://localhost:5173,https://keeper-shwetank.vercel.app
# Or if your code reads CORS_ORIGIN instead:
# CORS_ORIGIN=http://localhost:5173,https://keeper-shwetank.vercel.app

# ===== Server =====
PORT=5000
